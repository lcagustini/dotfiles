require("bufferline").setup {
    options = {
        show_buffer_close_icons = false,
        separator_style = "slant"
    },
    highlights = {
        buffer_selected = {
            italic = false,
        }
    }
}
